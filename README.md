# FamilyFinance - Docker Deployment

## 📋 Описание

Этот документ содержит инструкции по развертыванию приложения FamilyFinance - системы управления семейным бюджетом с использованием Docker и docker-compose.

## 🚀 Быстрый старт

### Предварительные требования

Перед началом работы убедитесь, что у вас установлены следующие программы:

- [Docker](https://www.docker.com/get-started) (версия 20.10 или выше)
- [Docker Compose](https://docs.docker.com/compose/install/) (версия 1.29 или выше)

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-repository/familyfinance.git
cd familyfinance
```

### 2. Сборка и запуск контейнеров

```bash
docker-compose up -d --build
```

Эта команда:
- Создаст Docker-образ для приложения
- Запустит контейнеры для приложения и базы данных PostgreSQL
- Применит необходимые настройки сети и томов

### 3. Проверка работы

После успешного запуска вы можете открыть приложение в браузере:

👉 [http://localhost:5000](http://localhost:5000)

## 🛠️ Команды управления

### Запуск приложения

```bash
docker-compose up -d
```

### Остановка приложения

```bash
docker-compose down
```

### Просмотр логов

```bash
docker-compose logs -f
```

### Пересборка контейнеров

```bash
docker-compose up -d --build --force-recreate
```

### Очистка (удаление контейнеров, сетей и томов)

```bash
docker-compose down -v
```

## 📦 Структура проекта

```
familyfinance/
├── Dockerfile              # Конфигурация Docker для приложения
├── docker-compose.yml      # Конфигурация docker-compose
├── client/                 # Фронтенд приложения
├── server/                 # Бэкенд приложения
├── shared/                 # Общие типы и схемы
└── package.json            # Зависимости проекта
```

## 🔧 Конфигурация

### Переменные окружения

Вы можете настроить приложение, изменив переменные окружения в файле `docker-compose.yml`:

- `NODE_ENV` - режим работы (production/development)
- `PORT` - порт, на котором будет работать приложение
- `POSTGRES_USER` - пользователь базы данных
- `POSTGRES_PASSWORD` - пароль пользователя базы данных
- `POSTGRES_DB` - имя базы данных

### Настройка базы данных

По умолчанию используется PostgreSQL 15 с следующими параметрами:

- **Пользователь:** `postgres`
- **Пароль:** `postgres`
- **База данных:** `familyfinance`
- **Порт:** `5432`

Вы можете изменить эти параметры в файле `docker-compose.yml`.

## 🧪 Разработка

Для разработки вы можете запустить приложение в режиме разработки:

```bash
docker-compose -f docker-compose.dev.yml up -d
```

*Примечание: Для разработки вам нужно создать отдельный файл `docker-compose.dev.yml` с настройками для режима разработки.*

## 🔒 Безопасность

- Не используйте стандартные учетные данные для продакшен-среды
- Измените пароли и имена пользователей перед развертыванием
- Настройте файрвол для ограничения доступа к портам

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробности смотрите в файле LICENSE.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы, пожалуйста, создайте issue в репозитории проекта.

---

🎉 **Готово!** Ваше приложение FamilyFinance успешно развернуто с помощью Docker.
